export const i18nConfig = {
    locales: ['en', 'vi'],
    defaultLocale: 'en'
};

const languages = [
    {locale: 'en', name: "English"},
    {locale: 'vi', name: "Tiếng Việt"}
]
export default languages;

